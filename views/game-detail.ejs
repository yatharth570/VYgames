<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head.ejs') %>
    <link rel="stylesheet" href="/css/game-detail.css"/>
</head>
<body>
    <%- include('includes/header.ejs') %>
    <div class="container">
        <div class="card">
            <div class="Background">

                <img src="/<%= product.image %>" alt="" class="game show" >
                

            </div>
            <div class="info">
                <div class="gameName">
                    <div>
                        <h1 class="big"><%= product.gameName %></h1>
                    </div>
                    <h3 class="small"><%= product.devName %></h3>
                </div>
                <div class="description">
                    <h3 class="title">Game Info</h3>
                    <p class="text"><%= product.desc %></p>
                </div>
                <a href="/products/<%= product._id %>" class="btn">Details</a>
        <% if (isAuthenticated) { %>
            <%- include('includes/add-to-cart.ejs', {product: product}) %>
        <% } %>
    
</body>
</html>

    